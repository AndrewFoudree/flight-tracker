name: Check flight prices

on:
  schedule:
    # Once a week: 8 searches a run, at most 40 a cycle against a 250 plan. When
    # the cycle runs short the run records NA rather than skipping silently, so a
    # blind stretch shows on the dashboard as a gap instead of looking like flat
    # prices.
    - cron: "0 13 * * 6"    # Saturday 13:00 UTC, 08:00 Central (07:00 once CST starts)
  workflow_dispatch:        # manual trigger for testing
    inputs:
      route:
        description: "Limit to these route ids, comma separated (blank runs all)"
        required: false
      dry_run:
        description: "Analyse stored history without calling any API"
        type: boolean
        default: false

permissions:
  contents: write           # commit the CSV
  issues: write             # open alert issues
  pages: write              # publish the refreshed dashboard
  id-token: write

# Two runs writing data/ at once would fight over the push.
concurrency:
  group: prices
  cancel-in-progress: false

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - run: pip install -r requirements.txt

      - name: Fetch and analyse
        env:
          SERPAPI_KEY: ${{ secrets.SERPAPI_KEY }}
          TRAVELPAYOUTS_TOKEN: ${{ secrets.TRAVELPAYOUTS_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PARTY: ${{ vars.PARTY }}
          ROUTES: ${{ inputs.route }}
          DRY_RUN: ${{ inputs.dry_run }}
        run: |
          # main.py takes --route repeatably; expanding the input here is what
          # makes that reachable. Built as a list rather than interpolated into
          # the command so a route id can never become shell.
          args=""
          if [ -n "$ROUTES" ]; then
            IFS=','
            for route in $ROUTES; do
              if [ -n "$route" ]; then args="$args --route $route"; fi
            done
            unset IFS
          fi
          if [ "$DRY_RUN" = "true" ]; then args="$args --dry-run"; fi
          python -m src.main $args

      - name: Commit results
        if: always()          # keep partial data even when the fetch reports failure
        run: |
          git config user.name "flight-bot"
          git config user.email "flight-bot@users.noreply.github.com"
          python scripts/commit_data.py "prices: $(date -u +%Y-%m-%d)"

  # A push made with GITHUB_TOKEN does not trigger other workflows, so the daily
  # price commit can never fire publish-dashboard.yml. Deploying here instead is
  # what actually keeps the published page in step with the data.
  publish:
    needs: check
    if: always()              # a blind run still redeploys the page it has
    runs-on: ubuntu-latest
    concurrency:
      group: pages
      cancel-in-progress: false
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main         # the tip, including the commit the check job just pushed

      - name: Stage the site
        run: |
          mkdir -p _site/data
          cp dashboard/* _site/
          # GitHub Pages serves assets with max-age=600, so a deploy is invisible
          # for ten minutes unless the URL changes. Stamping the commit onto the
          # script src makes every publish take effect on the next load instead
          # of leaving people hard-refreshing to see their own data.
          sed -i "s|src=\"chart.js\"|src=\"chart.js?v=${GITHUB_SHA}\"|" _site/index.html
          cp data/prices.csv data/routes.json _site/data/
          # Optional, and separate so a missing one never masks a missing price
          # file: runs.csv does not exist until the first run records an outcome.
          cp data/runs.csv _site/data/ 2>/dev/null || true

      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site
      - id: deploy
        uses: actions/deploy-pages@v4
